/*const express = require('express');
const router = express.Router();
const { createSale } = require('../controllers/salesController'); // Adjust the path as needed

// Create a new sale
router.post('/', createSale);

module.exports = router;
*/